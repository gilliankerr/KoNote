# KoNote2 Demo Configuration
#
# This file makes it easy to try KoNote2 without any setup.
#
# Usage:
#   docker-compose -f docker-compose.demo.yml up
#
# Then open http://localhost:8000 in your browser.
#
# WARNING: This configuration uses default keys and is NOT SECURE.
# Do NOT use for real client data. For production, use docker-compose.yml
# with proper environment variables.

services:
  web:
    build: .
    environment:
      - DJANGO_SETTINGS_MODULE=konote.settings.production
      - KoNote2_MODE=demo
      - DATABASE_URL=postgresql://KoNote2:demo_password_123@db:5432/KoNote2
      - AUDIT_DATABASE_URL=postgresql://audit:audit_password_123@audit_db:5432/KoNote2_audit
      - SECRET_KEY=demo-only-secret-key-do-not-use-in-production-abc123
      - FIELD_ENCRYPTION_KEY=ly6OqAlMm32VVf08PoPJigrLCIxGd_tW1-kfWhXxXj8=
      - AUTH_MODE=local
      - ALLOWED_HOSTS=localhost,127.0.0.1
      - DEBUG=False
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      audit_db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    volumes:
      - demo_pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=KoNote2
      - POSTGRES_USER=KoNote2
      - POSTGRES_PASSWORD=demo_password_123
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U KoNote2"]
      interval: 5s
      timeout: 5s
      retries: 5

  audit_db:
    image: postgres:16-alpine
    volumes:
      - demo_audit_pgdata:/var/lib/postgresql/data
      - ./scripts/audit_db_init.sql:/docker-entrypoint-initdb.d/01-init.sql
    environment:
      - POSTGRES_DB=KoNote2_audit
      - POSTGRES_USER=audit
      - POSTGRES_PASSWORD=audit_password_123
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U audit -d KoNote2_audit"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  demo_pgdata:
  demo_audit_pgdata:
