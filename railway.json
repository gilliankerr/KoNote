{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate 2>&1; python manage.py migrate --database=audit 2>&1; python manage.py seed 2>&1; echo 'Starting gunicorn on port '$PORT; gunicorn konote.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --log-level debug --error-logfile - --access-logfile -",
    "restartPolicyType": "ON_FAILURE"
  },
  "watchPatterns": ["*.py", "*.html", "*.css", "*.js", "requirements.txt", "Dockerfile"]
}
